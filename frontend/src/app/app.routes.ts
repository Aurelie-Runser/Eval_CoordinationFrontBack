import { Routes } from '@angular/router';
import { HomeTitleComponent } from './components/home-title/home-title';
import { CommandeList } from './components/commande-list/commande-list';

export const routes: Routes = [
//   { path: '', component: HomeTitleComponent },
  { path: '', component: CommandeList },
];
